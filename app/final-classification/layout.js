// app/final-classification/layout.js
export const metadata = {
  title: "Jersey Predictions",
  description: "Predict the Red, Green, Polka Dot and White jersey winners of La Vuelta a España 2026.",
};

export default function FinalClassificationLayout({ children }) {
  return children;
}
