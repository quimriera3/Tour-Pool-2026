// app/women/page.js
// Same Dashboard component as the men's homepage — it resolves which race it
// is rendering from the URL via useRace(), so there is one implementation.
export { default } from "../page";
