// app/predictions/layout.js
export const metadata = {
  title: "Stage Predictions",
  description: "Pick the winner of every 2026 Tour de France stage. Riders ranked by favourite for each stage profile.",
};

export default function PredictionsLayout({ children }) {
  return children;
}
