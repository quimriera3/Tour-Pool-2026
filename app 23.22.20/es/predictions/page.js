// app/es/predictions/page.js
//
// Same component as app/predictions/page.js -- it detects the language from
// the URL itself (useLang() checks for the "/es" prefix), so there's only
// one real implementation to maintain.
export { default } from "../../predictions/page";
