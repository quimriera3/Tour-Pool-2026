// app/faq/layout.js
export const metadata = {
  title: "FAQ — La Vuelta Pool 2026 | Grand Tour Pool",
  description:
    "Answers to the most common questions about Grand Tour Pool and La Vuelta a España 2026: how scoring works, when picks lock, jersey predictions, and everything about the race.",
};

export default function FAQLayout({ children }) {
  return children;
}
